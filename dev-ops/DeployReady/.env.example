# Application Environment Variables
# Default values shown — adjust as needed for your deployment

# Port on which the application will listen
PORT=3000

# Node.js environment (development, production, staging)
NODE_ENV=production
